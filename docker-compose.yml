version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: codeFolio-server
    ports:
      - "${SPRING_PORT}:8080"
    env_file:
      - ${ENV_FILE:-.env.local}
    environment:
      - SPRING_PROFILES_ACTIVE=${SPRING_PROFILES_ACTIVE}
    depends_on:
      - codeFolio-database
    restart: always
    networks:
      - codefolio-net

  # 로컬에서만 MySQL 컨테이너 구동
  codeFolio-database:
    image: mysql:8.4.4
    container_name: codeFolio-database
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD}
      MYSQL_DATABASE: ${DB_DATABASE}
      MYSQL_USER: ${DB_USERNAME}
      MYSQL_PASSWORD: ${DB_PASSWORD}
    ports:
      - "${DB_PORT}:3306"
    volumes:
      - codefolio-data:/var/lib/mysql
    restart: always
    networks:
      - codefolio-net
    profiles: ["local"]

volumes:
  codefolio-data:

networks:
  codefolio-net:
    driver: bridge
